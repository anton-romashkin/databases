SELECT SUM(tbl.likes) AS total_likes_of_10_youngest
FROM
(SELECT
  COUNT(likes.id) AS likes,
  profiles.birthday AS birthday
FROM likes
JOIN profiles
ON likes.target_id = profiles.user_id
GROUP BY profiles.birthday
ORDER BY profiles.birthday DESC
LIMIT 10) AS tbl;
